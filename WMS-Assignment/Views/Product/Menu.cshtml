@model MenuItemVM

@{
    ViewBag.Title = "Menu";
}

<form data-ajax="true"
      data-ajax-update="#target"
      data-ajax-loading="#loader">

    @Html.TextBox("name", "",
                  new { id = "name" , type = "search", autofocus = "", data_trim = "",
                  placeholder = "Search For Product" })

</form>

<div id="target">
    <partial name="_A" model="Model">
    </div>

    
   @section foot {
        <script>
            let timer = null;

            $('#name').on('input', e => {
                clearTimeout(timer);
                timer = setTimeout(() => $(e.target.form).submit(), 500);
            });
        </script>
    }
    
